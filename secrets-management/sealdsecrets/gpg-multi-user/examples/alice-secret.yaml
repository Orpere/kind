# Example SealedSecret — Sealed by Alice (alice@corp.com)
# Namespace scope: strict (default)
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: db-credentials
  namespace: myapp
  labels:
    app: myapp
    sealed-by: alice@corp.com
  annotations:
    sealedsecrets.bitnami.com/namespace-wide: "false"
spec:
  encryptedData:
    # Encrypted with Alice's GPG public key
    # This value is only decryptable by the controller that holds Alice's
    # corresponding private key (or any key in the trusted key ring).
    password: AgBy3i4OJSWK+PiTySYZZA9rO43cGDEq...
    username: AgBy3i4OJSWK+PiTySYZZA9rO43cGDEq...
  template:
    type: Opaque
    metadata:
      name: db-credentials
      namespace: myapp
      labels:
        app: myapp
        environment: development
        sealed-by: alice@corp.com
